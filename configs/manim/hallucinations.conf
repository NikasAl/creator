# Config for: Человечий мозг галлюцинирует так же, как GPT: почему это нормально и что с этим делать
BASE_DIR="pipelines_manim/hallucinations"
TITLE="Человечий мозг галлюцинирует так же, как GPT: почему это нормально и что с этим делать"
AUTHOR="Nikas"
LANGUAGE="ru"

# Модели
SCRIPT_MODEL="custom"    # Генерация текста урока (GPT-4o / Claude 3.5)
CODE_MODEL="custom"      # Генерация кода Manim
PROMO_MODEL="default"     # Генерация промо

# Флаги рендеринга
QUALITY="low"             # low, medium, high, 4k
