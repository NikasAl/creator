#!/usr/bin/env python3
"""
Демонстрационный скрипт для тестирования бесплатных решений транскрибации
"""

import sys
import os
from pathlib import Path

# Добавляем пути для импорта
sys.path.append(str(Path(__file__).parent.parent))

from audio_processors.free_transcriber import FreeTranscriber


def demo_free_transcription():
    """Демонстрация бесплатной транскрибации"""
    print("🎤 ДЕМОНСТРАЦИЯ БЕСПЛАТНОЙ ТРАНСКРИБАЦИИ")
    print("=" * 50)
    
    # Создаем тестовый текст
    test_text = """Фрагмент 1

Введение в психоанализ и шизоидные явления

Эта книга Гарри Гантрипа посвящена изучению шизоидных явлений и их связи с психоанализом. Автор исследует, как проблемы, возникающие в раннем детстве, влияют на формирование личности и создают трудности в психотерапии.

Фрагмент 2

Психоанализ: от Фрейда к целостному пониманию личности

Психоанализ, начавшийся с идей Зигмунда Фрейда, долгое время фокусировался на изучении влечений и механизмов психики. Однако со временем стало ясно, что для понимания человека важно рассматривать его как целостную личность.
"""
    
    # Сохраняем тестовый текст
    test_text_file = "demo_text.txt"
    with open(test_text_file, 'w', encoding='utf-8') as f:
        f.write(test_text)
    
    print(f"📝 Создан тестовый текст: {test_text_file}")
    
    # Создаем фиктивный аудио файл
    test_audio = "demo_audio.mp3"
    with open(test_audio, 'wb') as f:
        # Создаем минимальный MP3 файл (заголовок)
        f.write(b'\xff\xfb\x90\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00')
    
    print(f"🎵 Создан тестовый аудио файл: {test_audio}")
    
    # Создаем транскрайбер
    transcriber = FreeTranscriber()
    
    print(f"\n🔧 Настройки бесплатного транскрайбера:")
    print(f"   - OpenRouter API: {'✅' if transcriber.openrouter_api_key else '❌'}")
    print(f"   - Hugging Face: {'✅' if transcriber.huggingface_token else '❌'}")
    print(f"   - Локальный Whisper: {'✅' if transcriber.use_local_whisper else '❌'}")
    
    # Пробуем транскрибацию
    print(f"\n🔄 Пробуем бесплатную транскрибацию...")
    
    try:
        # Пробуем OpenRouter (бесплатный лимит)
        if transcriber.openrouter_api_key:
            print("🎤 Тестируем OpenRouter транскрибацию (бесплатный лимит)...")
            result = transcriber.transcribe_with_openrouter_free(test_audio)
            if result:
                print("✅ OpenRouter транскрибация работает!")
            else:
                print("❌ OpenRouter транскрибация не работает или лимит исчерпан")
        
        # Пробуем Hugging Face
        if transcriber.huggingface_token:
            print("🎤 Тестируем Hugging Face транскрибацию...")
            result = transcriber.transcribe_with_huggingface(test_audio)
            if result:
                print("✅ Hugging Face транскрибация работает!")
            else:
                print("❌ Hugging Face транскрибация не работает")
        
        # Пробуем локальный Whisper
        if transcriber.use_local_whisper:
            print("🎤 Тестируем локальную транскрибацию...")
            result = transcriber.transcribe_with_local_whisper(test_audio)
            if result:
                print("✅ Локальная транскрибация работает!")
            else:
                print("❌ Локальная транскрибация не работает")
        
        # Тестируем синхронизацию
        print(f"\n🔄 Тестируем синхронизацию текста с аудио...")
        
        output_file = "demo_aligned.json"
        success = transcriber.align_text_with_audio(test_text_file, test_audio, output_file, "auto")
        
        if success:
            print(f"✅ Синхронизация завершена: {output_file}")
            
            # Показываем результат
            import json
            with open(output_file, 'r', encoding='utf-8') as f:
                data = json.load(f)
            
            print(f"📊 Результат синхронизации:")
            print(f"   - Фрагментов: {len(data.get('aligned_content', []))}")
            print(f"   - Сегментов: {len(data.get('segments', []))}")
            print(f"   - Метод: {data.get('metadata', {}).get('transcription_method', 'unknown')}")
            print(f"   - Бесплатный сервис: {data.get('metadata', {}).get('free_service', False)}")
            
            # Показываем пример фрагмента
            if data.get('aligned_content'):
                fragment = data['aligned_content'][0]
                print(f"\n📋 Пример фрагмента:")
                print(f"   Номер: {fragment['fragment_number']}")
                print(f"   Время: {fragment['start_time']:.1f} - {fragment['end_time']:.1f} сек")
                print(f"   Длительность: {fragment['duration']:.1f} сек")
                print(f"   Текст: {fragment['text'][:100]}...")
            
            # Очищаем результат
            os.remove(output_file)
        else:
            print("❌ Синхронизация не удалась")
    
    except Exception as e:
        print(f"❌ Ошибка при тестировании: {e}")
    
    # Очищаем тестовые файлы
    os.remove(test_text_file)
    os.remove(test_audio)
    
    print(f"\n🧹 Тестовые файлы удалены")


def demo_free_pipeline():
    """Демонстрация бесплатного пайплайна"""
    print("\n🎬 ДЕМОНСТРАЦИЯ БЕСПЛАТНОГО ПАЙПЛАЙНА")
    print("=" * 50)
    
    # Создаем тестовые файлы
    test_summary = """Фрагмент 1

Введение в психоанализ

Эта книга посвящена изучению психоаналитических концепций и их практическому применению в терапии.

Фрагмент 2

Основные принципы

Психоанализ основан на понимании бессознательных процессов и их влияния на поведение человека.
"""
    
    test_summary_file = "demo_summary.txt"
    with open(test_summary_file, 'w', encoding='utf-8') as f:
        f.write(test_summary)
    
    test_audio = "demo_audio.mp3"
    with open(test_audio, 'wb') as f:
        f.write(b'\xff\xfb\x90\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00')
    
    print(f"📝 Создан тестовый summary: {test_summary_file}")
    print(f"🎵 Создан тестовый аудио: {test_audio}")
    
    # Импортируем и тестируем бесплатный пайплайн
    try:
        from video_processors.free_video_pipeline import FreeVideoPipeline
        
        pipeline = FreeVideoPipeline()
        
        print(f"\n🚀 Запуск бесплатного пайплайна...")
        
        # Запускаем без транскрибации для демо
        success = pipeline.run_pipeline(
            test_summary_file,
            test_audio,
            "demo_free_output",
            "artistic",
            False  # Без транскрибации
        )
        
        if success:
            print(f"✅ Бесплатный пайплайн завершен успешно!")
            
            # Показываем созданные файлы
            output_path = Path("demo_free_output")
            if output_path.exists():
                for file_path in output_path.glob("*"):
                    size = file_path.stat().st_size
                    print(f"   📄 {file_path.name} ({size:,} байт)")
            
            # Очищаем результат
            import shutil
            shutil.rmtree("demo_free_output")
        else:
            print("❌ Ошибка при выполнении пайплайна")
    
    except ImportError as e:
        print(f"⚠️  Не удалось импортировать FreeVideoPipeline: {e}")
        print("   Это нормально для демо-режима")
    
    # Очищаем тестовые файлы
    os.remove(test_summary_file)
    os.remove(test_audio)
    
    print(f"\n🧹 Тестовые файлы удалены")


def show_free_alternatives():
    """Показывает бесплатные альтернативы"""
    print("\n🆓 БЕСПЛАТНЫЕ АЛЬТЕРНАТИВЫ")
    print("=" * 50)
    
    print("""
### Транскрибация (бесплатно):

1. **OpenRouter** - бесплатный лимит
   - До 25MB файлы
   - Использует Whisper
   - Требует API ключ

2. **Hugging Face** - полностью бесплатно
   - Модели Whisper
   - Требует токен (бесплатный)
   - Ограничения по времени

3. **Локальный Whisper** - полностью бесплатно
   - Установка: pip install openai-whisper
   - Работает офлайн
   - Требует больше ресурсов

4. **Простая синхронизация** - без транскрибации
   - Равномерное распределение
   - Не требует API
   - Менее точная

### Генерация изображений (бесплатно):

1. **Stable Diffusion WebUI** - локально
   - Полностью бесплатно
   - Требует установки
   - Высокое качество

2. **Leonardo.ai** - веб-сервис
   - 150 изображений бесплатно
   - Простая регистрация
   - Хорошее качество

3. **Bing Image Creator** - Microsoft
   - 25 изображений бесплатно
   - Требует аккаунт Microsoft
   - Быстрая генерация

### Рекомендации:

1. **Для транскрибации**: Начните с OpenRouter, затем Hugging Face
2. **Для изображений**: Stable Diffusion WebUI для лучшего качества
3. **Для экономии**: Используйте простую синхронизацию без транскрибации
""")


def main():
    """Главная функция демонстрации"""
    print("🎤 ДЕМОНСТРАЦИЯ БЕСПЛАТНЫХ РЕШЕНИЙ ТРАНСКРИБАЦИИ")
    print("=" * 60)
    print("Этот скрипт демонстрирует возможности бесплатной транскрибации")
    print("и синхронизации текста с аудио для создания видео.")
    print()
    
    try:
        # Демонстрация бесплатной транскрибации
        demo_free_transcription()
        
        # Демонстрация бесплатного пайплайна
        demo_free_pipeline()
        
        # Показываем бесплатные альтернативы
        show_free_alternatives()
        
        print("\n" + "=" * 60)
        print("🎉 ДЕМОНСТРАЦИЯ БЕСПЛАТНЫХ РЕШЕНИЙ ЗАВЕРШЕНА!")
        print("=" * 60)
        print("Теперь вы можете использовать эти функции с реальными файлами:")
        print()
        print("1. Бесплатная транскрибация:")
        print("   python audio_processors/free_transcriber.py text.txt audio.mp3")
        print()
        print("2. Бесплатный видео-пайплайн:")
        print("   python video_processors/free_video_pipeline.py summary.txt audio.mp3")
        print()
        print("3. Бесплатный пайплайн без транскрибации:")
        print("   python video_processors/free_video_pipeline.py summary.txt audio.mp3 --no-transcription")
        print()
        print("4. Установка локального Whisper:")
        print("   pip install openai-whisper")
        print("   export USE_LOCAL_WHISPER=true")
        
    except Exception as e:
        print(f"\n❌ Ошибка в демонстрации: {e}")
        return 1
    
    return 0


if __name__ == "__main__":
    exit(main()) 