# Генератор видео с динамическими эффектами камеры

## Описание

Обновленный генератор видео создает MP4 файлы из набора изображений и аудио с плавными эффектами движения камеры. Камера плавно движется по изображениям, создавая динамичные и интересные переходы.

## Ключевые особенности

### 🎬 Динамические эффекты движения
- **Плавное движение**: Камера движется от начальной позиции к конечной в течение времени показа изображения
- **Безопасные границы**: Камера никогда не выходит за пределы изображения
- **Разнообразные переходы**: 12 различных типов эффектов для создания интересных комбинаций

### 📱 Доступные эффекты

| Эффект | Описание |
|--------|----------|
| `zoom_in` | Плавное приближение от 1.0x до 1.3x |
| `zoom_out` | Плавное отдаление от 1.3x до 0.9x |
| `pan_left` | Движение влево от центра к левому краю |
| `pan_right` | Движение вправо от центра к правому краю |
| `pan_up` | Движение вверх от центра к верхнему краю |
| `pan_down` | Движение вниз от центра к нижнему краю |
| `zoom_pan` | Комбинированный эффект: zoom + pan по диагонали |
| `circular_pan` | Круговое движение камеры по центру |
| `diagonal_pan` | Диагональное движение по изображению |
| `random_movement` | Случайное движение с zoom и pan |
| `slow_zoom` | Медленное и плавное приближение |
| `bounce_zoom` | Эффект "отскока" - приближение, затем отдаление |

## Использование

### Базовое использование
```bash
python video_generator.py --pipeline-dir /path/to/pipeline
```

### Просмотр доступных эффектов
```bash
python video_generator.py --list-effects
```

### Создание демо-видео со всеми эффектами
```bash
python video_generator.py --pipeline-dir /path/to/pipeline --preview
```

### Указание конкретных эффектов
```bash
python video_generator.py --pipeline-dir /path/to/pipeline \
    --effects zoom_in pan_left circular_pan zoom_out
```

### Настройка параметров
```bash
python video_generator.py --pipeline-dir /path/to/pipeline \
    --fade-duration 0.8 \
    --quality high \
    --output custom_video.mp4
```

## Структура пайплайна

Генератор ожидает следующую структуру каталога:
```
pipeline_dir/
├── images/
│   ├── illustration_1.png
│   ├── illustration_2.png
│   └── ...
├── audio.mp3
└── illustrations.json
```

## Технические детали

### Алгоритм работы
1. **Анализ**: Определение длительности аудио и количества изображений
2. **Расчет времени**: Равномерное распределение времени между изображениями
3. **Создание клипов**: Генерация отдельных видео клипов для каждого изображения с выбранным эффектом
4. **Объединение**: Конкатенация всех клипов в финальное видео с аудио дорожкой

### Используемые фильтры FFmpeg
- **zoompan**: Основной фильтр для создания эффектов движения
- **fade**: Плавные переходы между изображениями
- **concat**: Объединение клипов

### Оптимизация
- **Пошаговый подход**: Избежание сложных фильтров для стабильности
- **Временные переменные**: Использование `t` для создания динамических эффектов
- **Безопасные границы**: Математические формулы гарантируют, что камера не выйдет за пределы

## Примеры команд

### Создание видео с случайными эффектами
```bash
python video_generator.py --pipeline-dir ./pipeline_Gantrip_150_200
```

### Создание видео с конкретными эффектами
```bash
python video_generator.py --pipeline-dir ./pipeline_Gantrip_150_200 \
    --effects slow_zoom circular_pan bounce_zoom
```

### Создание демо для тестирования эффектов
```bash
python video_generator.py --pipeline-dir ./pipeline_Gantrip_150_200 \
    --preview --demo-duration 2.0
```

## Требования

- Python 3.7+
- FFmpeg (должен быть доступен в PATH)
- Зависимости из `requirements.txt`

## Устранение неполадок

### Ошибка "ffmpeg не найден"
Убедитесь, что FFmpeg установлен и доступен в PATH:
```bash
ffmpeg -version
```

### Ошибка создания клипа
Проверьте:
- Наличие изображений в формате PNG
- Корректность пути к каталогу
- Права доступа к файлам

### Низкое качество видео
Используйте опцию `--quality high` для лучшего качества:
```bash
python video_generator.py --pipeline-dir /path/to/pipeline --quality high
```

## Разработка

### Добавление новых эффектов
1. Добавьте новый эффект в enum `CameraEffect`
2. Реализуйте логику в методе `create_single_clip`
3. Обновите документацию и тесты

### Тестирование
Запустите тестовый скрипт:
```bash
python test_video_generator.py
```

## Лицензия

Проект использует MIT лицензию.
