# 🎬 Система публикации видео на видеохостинги

Модульная система для автоматической публикации видео из пайплайнов на различные видеохостинги и платформы.

## ✨ Возможности

- **YouTube публикация** - полная поддержка загрузки видео с метаданными
- **VK публикация** - поддержка загрузки видео и аудио на VK Video и VK Audio
- **Анализ пайплайна** - автоматическое извлечение метаданных из готовых пайплайнов
- **LLM генерация** - создание привлекательных названий, описаний и тегов
- **Модульная архитектура** - легко расширяется новыми платформами
- **Валидация данных** - проверка метаданных перед публикацией
- **Обработка ошибок** - подробные сообщения об ошибках

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации
```bash
cp config.publisher.env.example config.publisher.env
# Отредактируйте config.publisher.env
```

### 3. Настройка YouTube API
- Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
- Включите YouTube Data API v3
- Создайте учетные данные OAuth 2.0
- Скачайте файл `youtube_credentials.json`

### 4. Настройка VK API
- Создайте приложение в [VK Developers](https://vk.com/dev)
- Получите `CLIENT_ID` и `CLIENT_SECRET`
- Получите токен доступа с правами `video`, `audio`, `wall`
- Укажите ID группы для публикации (опционально)

### 5. Аутентификация
```bash
python setup_youtube_auth.py
```

### 6. Проверка
```bash
python test_youtube_auth.py
```

### 7. Демонстрация
```bash
./demo_publisher.sh
```

## 📋 Основные команды

### Пробный запуск (без публикации)
```bash
python publisher.py pipeline_LemEng_87_111 --platforms youtube --dry-run
python publisher.py pipeline_LemEng_87_111 --platforms vk --dry-run
python publisher.py pipeline_LemEng_87_111 --platforms youtube vk --dry-run
```

### Публикация с автоматическими метаданными
```bash
python publisher.py pipeline_LemEng_87_111 --platforms youtube --privacy private
python publisher.py pipeline_LemEng_87_111 --platforms vk
python publisher.py pipeline_LemEng_87_111 --platforms youtube vk
```

### Публикация с пользовательскими метаданными
```bash
python publisher.py pipeline_LemEng_87_111 \
  --platforms youtube \
  --title "Мое видео" \
  --description "Описание видео" \
  --tags "тег1" "тег2" "тег3" \
  --privacy public

python publisher.py pipeline_LemEng_87_111 \
  --platforms vk \
  --title "Мое видео" \
  --description "Описание видео"
```

### Публикация без LLM
```bash
python publisher.py pipeline_LemEng_87_111 --platforms youtube --no-llm
python publisher.py pipeline_LemEng_87_111 --platforms vk --no-llm
```

## 🏗️ Архитектура

```
publishers/
├── __init__.py                 # Инициализация модуля
├── base_publisher.py           # Базовый класс публикатора
├── youtube_publisher.py        # Публикатор для YouTube
├── vk_publisher.py             # Публикатор для VK Video/Audio
├── pipeline_analyzer.py        # Анализатор пайплайна
├── llm_metadata_generator.py   # Генератор метаданных
└── README.md                   # Подробная документация

publisher.py                    # Основной скрипт публикации
test_publisher.py              # Тестовый скрипт
setup_youtube_auth.py          # Настройка аутентификации
test_youtube_auth.py            # Проверка аутентификации
demo_publisher.sh              # Демонстрационный скрипт
config.publisher.env.example   # Шаблон конфигурации
```

## 🔧 Конфигурация

### Основные параметры
- `OPENROUTER_API_KEY` - ключ для LLM генерации
- `YOUTUBE_CREDENTIALS_PATH` - путь к файлу учетных данных YouTube
- `VK_CLIENT_ID` - ID приложения VK
- `VK_CLIENT_SECRET` - секретный ключ приложения VK
- `VK_ACCESS_TOKEN` - токен доступа VK
- `VK_GROUP_ID` - ID группы для публикации (опционально)
- `DEFAULT_PRIVACY` - приватность по умолчанию
- `MAX_TITLE_LENGTH` - максимальная длина названия
- `MAX_DESCRIPTION_LENGTH` - максимальная длина описания

### Настройки LLM
- `DEFAULT_MODEL` - модель для генерации метаданных
- `DEFAULT_MAX_TOKENS` - максимальное количество токенов
- `DEFAULT_TEMPERATURE` - температура генерации

## 📊 Поддерживаемые платформы

### ✅ YouTube
- Полная поддержка загрузки видео
- Автоматическая генерация метаданных
- Загрузка превью
- Настройка приватности
- Получение информации о канале

### ✅ VK Video/Audio
- Поддержка загрузки видео на VK Video
- Поддержка загрузки аудио на VK Audio
- Автоматическое определение типа контента (audio.mp3/video.mp4)
- Публикация в группу
- Настройка приватности
- Получение информации о группе

### 🔄 Планируемые платформы
- **Rutube** - публикация на Rutube
- **Pikabu** - публикация статей с ссылками на видео
- **Dzen** - публикация статей с ссылками на видео

## 🛠️ Расширение системы

Для добавления новой платформы:

1. Создайте новый класс, наследующий от `BasePublisher`
2. Реализуйте методы:
   - `authenticate()` - аутентификация
   - `upload_video()` - загрузка видео
   - `update_video_metadata()` - обновление метаданных
   - `get_upload_status()` - статус загрузки

3. Добавьте поддержку в `publisher.py`

## 🔍 Примеры использования

### Анализ пайплайна
```python
from publishers.pipeline_analyzer import PipelineAnalyzer

analyzer = PipelineAnalyzer("pipeline_LemEng_87_111")
metadata = analyzer.analyze()
print(analyzer.get_summary())
```

### Генерация метаданных
```python
from publishers.llm_metadata_generator import LLMMetadataGenerator

generator = LLMMetadataGenerator("config.publisher.env")
title = generator.generate_title(content, book_title, book_author)
description = generator.generate_description(content, book_title, book_author)
tags = generator.generate_tags(content, book_title, book_author)
```

### Публикация на YouTube
```python
from publishers.youtube_publisher import YouTubePublisher
from publishers.base_publisher import VideoMetadata

publisher = YouTubePublisher("config.publisher.env")
publisher.authenticate()

metadata = VideoMetadata(
    title="Мое видео",
    description="Описание видео",
    tags=["тег1", "тег2"],
    video_path="video.mp4",
    privacy="private"
)

video_id = publisher.upload_video(metadata)
```

### Публикация на VK
```python
from publishers.vk_publisher import VKPublisher
from publishers.base_publisher import VideoMetadata

publisher = VKPublisher("config.publisher.env")
publisher.authenticate()

metadata = VideoMetadata(
    title="Мое видео",
    description="Описание видео",
    tags=["тег1", "тег2"],
    video_path="video.mp4",
    privacy="private"
)

# Загрузка только видео
video_id = publisher.upload_video(metadata)

# Загрузка только аудио
audio_id = publisher.upload_audio(metadata)

# Загрузка и видео, и аудио
results = publisher.upload_both(metadata)
video_id = results['video_id']
audio_id = results['audio_id']
```

## 🚨 Устранение неполадок

### Ошибка аутентификации YouTube
1. **Токен истек:**
   ```bash
   rm youtube_token.json
   python setup_youtube_auth.py
   ```

2. **Недостаточно прав доступа:**
   - Убедитесь, что YouTube Data API v3 включен
   - Проверьте области доступа в Google Cloud Console

3. **Неправильные учетные данные:**
   - Проверьте файл `youtube_credentials.json`
   - Убедитесь, что client_id и client_secret правильные

4. **Проверка аутентификации:**
   ```bash
   python test_youtube_auth.py
   ```

### Ошибка аутентификации VK
1. **Токен истек или недействителен:**
   - Получите новый токен доступа в [VK API](https://vk.com/dev)
   - Убедитесь, что токен имеет права `video`, `audio`, `wall`

2. **Недостаточно прав доступа:**
   - Проверьте права токена в настройках приложения
   - Убедитесь, что приложение имеет доступ к Video API и Audio API

3. **Неправильные учетные данные:**
   - Проверьте `VK_CLIENT_ID` и `VK_CLIENT_SECRET` в конфигурации
   - Убедитесь, что ID группы указан правильно (если используется)

4. **Проверка аутентификации:**
   ```bash
   python -c "from publishers.vk_publisher import VKPublisher; VKPublisher().authenticate()"
   ```

### Ошибка генерации метаданных
1. Проверьте `OPENROUTER_API_KEY` в конфигурации
2. Убедитесь в доступности интернета
3. Проверьте лимиты API

### Ошибка загрузки видео
1. **YouTube:**
   - Проверьте размер файла (лимит: 128GB)
   - Убедитесь в корректности формата видео
   - Проверьте права доступа к файлу

2. **VK:**
   - Проверьте размер файла (лимит: 2GB для видео, 200MB для аудио)
   - Убедитесь в корректности формата (MP4 для видео, MP3 для аудио)
   - Проверьте права доступа к файлу
   - Убедитесь, что файлы audio.mp3 и video.mp4 существуют в пайплайне

## 📈 Планы развития

- **Пакетная публикация** - публикация нескольких видео одновременно
- **Планировщик** - автоматическая публикация по расписанию
- **Аналитика** - отслеживание статистики публикаций
- **Шаблоны** - предустановленные шаблоны метаданных
- **Интеграция с другими сервисами** - автоматическое создание постов в соцсетях

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи ошибок
2. Убедитесь в корректности конфигурации
3. Проверьте доступность API
4. Обратитесь к документации платформы

## 📄 Лицензия

Этот проект является частью системы автоматизации создания аудиокниг и видео.
