# Alibaba Video Generator

Генератор видеофрагментов через Alibaba Cloud Model Studio WAN API для создания анимированных видео из статичных изображений.

## Возможности

- 🎬 Генерация видео из изображений через image-to-video модель `wan2.1-i2v-turbo`
- 🤖 Автоматическая генерация промптов через LLM на основе текста стихов и описаний иллюстраций
- ⚙️ Гибкая настройка параметров видео (длительность, FPS, разрешение)
- 🔄 Интеграция с существующим `video_generator.py`
- 💰 Экономичная генерация только для выбранных изображений

## Установка и настройка

### 1. Установка зависимостей

```bash
pip install requests python-dotenv
```

### 2. Настройка конфигурации

Добавьте в `config.env`:

```env
# Alibaba Cloud Model Studio Configuration
ALIBABA_API_KEY=your_alibaba_api_key_here
ALIBABA_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
ALIBABA_VIDEO_MODEL=wan2.1-i2v-turbo
ALIBABA_PROMPT_MODEL=qwen/qwen3-30b-a3b:free
```

### 3. Получение API ключа

1. Зарегистрируйтесь на [Alibaba Cloud Model Studio](https://dashscope.console.aliyun.com/)
2. Получите API ключ в разделе API Keys
3. Добавьте ключ в `config.env`

## Использование

### Базовое использование

```bash
python video_processors/alibaba_video_generator.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --image-index 1
```

### Расширенные параметры

```bash
python video_processors/alibaba_video_generator.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --image-index 1 \
    --duration 6 \
    --fps 24 \
    --resolution 720p \
    --no-confirm
```

### Параметры командной строки

- `--pipeline-dir` - Каталог пайплайна с `images/`, `song.txt`, `illustrations.json`
- `--image-index` - Номер изображения для генерации видео (обязательный)
- `--duration` - Длительность видео в секундах (2-8, по умолчанию 4)
- `--fps` - Кадров в секунду (по умолчанию 24)
- `--resolution` - Разрешение видео: `480p`, `720p`, `1080p` (по умолчанию 720p)
- `--no-confirm` - Не запрашивать подтверждение перед генерацией
- `--config` - Путь к файлу конфигурации

## Рабочий процесс

### 1. Генерация видеофрагментов

```bash
# Генерируем видео для первого изображения
python video_processors/alibaba_video_generator.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --image-index 1

# Генерируем видео для второго изображения
python video_processors/alibaba_video_generator.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --image-index 2
```

### 2. Создание финального видео

После генерации видеофрагментов используйте существующий `video_generator.py`:

```bash
python video_processors/video_generator.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --output video.mp4
```

Готовые видео клипы (`video_01.mp4`, `video_02.mp4`, etc.) будут автоматически использованы вместо статичных изображений.

## Структура файлов

```
pipeline_ЗимнийВечер2/
├── song.txt                    # Текст стихов
├── illustrations.json          # Описания иллюстраций
├── audio.mp3                  # Аудио дорожка
└── images/
    ├── illustration_01.png    # Статичные изображения
    ├── illustration_02.png
    ├── video_01.mp4           # Сгенерированные видео (опционально)
    └── video_02.mp4
```

## Тестирование

Запустите тест интеграции:

```bash
python test_alibaba_integration.py \
    --pipeline-dir pipeline_ЗимнийВечер2 \
    --image-index 1
```

## Примеры промптов

Скрипт автоматически генерирует промпты на основе:

- **Текста стихов** - для понимания атмосферы и настроения
- **Описания иллюстраций** - для понимания сцены
- **Скрипта** - для понимания контекста

Примеры сгенерированных промптов:
- "Камера медленно приближается к окну, показывая, как снежинки танцуют в свете свечи"
- "Ветер колышет занавески, а тени от свечи создают таинственные узоры на стенах"
- "Буря за окном усиливается, молнии освещают лицо старушки, сидящей у окна"

## Ограничения и рекомендации

### Ограничения API
- Максимальная длительность: 8 секунд
- Поддерживаемые разрешения: 480p, 720p, 1080p
- Формат изображений: PNG, JPEG

### Рекомендации
- Генерируйте видео только для ключевых изображений (дорого)
- Используйте разрешение 720p для баланса качества и размера
- Длительность 4-6 секунд оптимальна для большинства сцен
- Проверяйте промпт перед генерацией

## Устранение неполадок

### Ошибка "ALIBABA_API_KEY не найден"
- Проверьте наличие ключа в `config.env`
- Убедитесь, что файл конфигурации загружается

### Ошибка "Изображение не найдено"
- Проверьте наличие файла `illustration_XX.png` в папке `images/`
- Убедитесь, что номер изображения корректен

### Ошибка генерации видео
- Проверьте интернет-соединение
- Убедитесь в корректности API ключа
- Попробуйте уменьшить длительность или разрешение

### Ошибка скачивания видео
- Проверьте доступность URL видео
- Убедитесь в наличии свободного места на диске
- Проверьте права на запись в папку `images/`

## Интеграция с существующим workflow

1. **Подготовка**: Убедитесь, что пайплайн содержит все необходимые файлы
2. **Генерация**: Создайте видеофрагменты для выбранных изображений
3. **Сборка**: Используйте `video_generator.py` для создания финального видео
4. **Результат**: Получите видео с автоматически интегрированными анимированными фрагментами

Готовые видео клипы будут автоматически использованы вместо статичных изображений при создании финального видео.
