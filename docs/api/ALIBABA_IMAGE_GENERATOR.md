# Alibaba Image Generator

Генератор изображений через Alibaba Cloud Model Studio WAN API для создания новых иллюстраций с использованием различных text-to-image моделей.

## Описание

Скрипт `alibaba_image_generator.py` позволяет генерировать новые изображения для существующих пайплайнов, используя промпты из файла `illustrations.json`. Это полезно когда:

- Полученные изображения имеют низкое качество
- Нужно попробовать другие модели генерации
- Требуется создать альтернативные варианты иллюстраций

## Поддерживаемые модели

- `wan2.5-t2i-preview` (рекомендуется) - Wan 2.5 preview
- `wan2.2-t2i-flash` (рекомендуется) - Wan 2.2 Flash Edition  
- `wan2.2-t2i-plus` (рекомендуется) - Wan 2.2 Professional Edition
- `wanx2.1-t2i-turbo` - Wan 2.1 Turbo Edition
- `wanx2.1-t2i-plus` - Wan 2.1 Professional Edition
- `wanx2.0-t2i-turbo` - Wan 2.0 Turbo Edition

## Настройка

### 1. Конфигурация API ключей

Добавьте в файл `config.env`:

```bash
# Alibaba Cloud API
ALIBABA_API_KEY=your_api_key_here
ALIBABA_BASE_URL=https://dashscope-intl.aliyuncs.com/api/v1
ALIBABA_IMAGE_MODEL=wan2.5-t2i-preview
```

### 2. Получение API ключа

1. Зарегистрируйтесь на [Alibaba Cloud Model Studio](https://www.alibabacloud.com/help/zh/model-studio/get-api-key)
2. Получите API ключ для Singapore региона
3. Добавьте ключ в конфигурацию

## Использование

### Базовое использование

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1,3,5
```

### Расширенные параметры

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1,2,3,4,5 \
    --size 1280*720 \
    --n 2 \
    --prompt-extend \
    --seed 42 \
    --no-confirm
```

## Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--pipeline-dir` | Каталог пайплайна с images/ и song.txt | Обязательный |
| `--indices` | Номера изображений для генерации (через запятую) | Обязательный |
| `--size` | Размер изображения (например: 1024*1024, 1280*720) | 1024*1024 |
| `--n` | Количество изображений на промпт (1-4) | 1 |
| `--prompt-extend` | Включить расширение промпта LLM | False |
| `--watermark` | Добавить водяной знак "AI Generated" | False |
| `--seed` | Seed для воспроизводимости результатов | None |
| `--no-confirm` | Не запрашивать подтверждение | False |
| `--config` | Путь к файлу конфигурации | config.env |

## Поддерживаемые размеры изображений

### wan2.5-t2i-preview
- Общее количество пикселей: 768×768 - 1440×1440
- Соотношение сторон: 1:4 - 4:1
- Примеры: 768×2700, 1280×1280, 1024×1024

### wan2.2 и более ранние
- Ширина и высота: 512 - 1440 пикселей
- Максимальное разрешение: 1440×1440
- Примеры: 1024×1024, 1280×720, 1024×1024

## Рекомендуемые размеры

| Соотношение | Размер |
|-------------|--------|
| 1:1 | 1280×1280 или 1024×1024 |
| 2:3 | 800×1200 |
| 3:2 | 1200×800 |
| 3:4 | 960×1280 |
| 4:3 | 1280×960 |
| 9:16 | 720×1280 |
| 16:9 | 1280×720 |
| 21:9 | 1344×576 |

## Примеры использования

### 1. Генерация одного изображения

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1
```

### 2. Генерация нескольких изображений

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1,3,5,7
```

### 3. Генерация с расширением промпта

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1,2,3 \
    --prompt-extend
```

### 4. Генерация нескольких вариантов

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1 \
    --n 4 \
    --size 1024*1024
```

### 5. Генерация с фиксированным seed

```bash
python video_processors/alibaba_image_generator.py \
    --pipeline-dir pipeline_ЦветокПушкинМНеороманс \
    --indices 1,2,3 \
    --seed 12345
```

## Структура файлов

После генерации в каталоге `images/` появятся новые файлы:

```
images/
├── illustration_01.png          # Оригинальное изображение
├── illustration_01_alibaba.png  # Новое изображение (n=1)
├── illustration_02_alibaba_1.png # Новые изображения (n>1)
├── illustration_02_alibaba_2.png
└── ...
```

## Ограничения

- **Время жизни**: task_id и URL изображений действительны 24 часа
- **Модерация контента**: Промпты и изображения проходят модерацию
- **Сетевой доступ**: Требуется доступ к OSS доменам Alibaba Cloud
- **Биллинг**: Оплата только за успешно сгенерированные изображения

## Устранение неполадок

### Ошибка аутентификации
```
❌ Ошибка API: 401 - Unauthorized
```
**Решение**: Проверьте правильность `ALIBABA_API_KEY`

### Ошибка модерации
```
❌ Ошибка API: 400 - DataInspectionFailed
```
**Решение**: Измените промпт, избегая запрещенного контента

### Превышение лимитов
```
❌ Ошибка API: 429 - RateLimitExceeded
```
**Решение**: Уменьшите частоту запросов, используйте интервалы

### Недоступность изображения
```
❌ Ошибка скачивания изображения: 404
```
**Решение**: URL истек (24 часа), повторите генерацию

## Статистика

Скрипт выводит статистику после завершения:

```
🎉 ГЕНЕРАЦИЯ ЗАВЕРШЕНА!
📊 Статистика:
   Изображений создано: 3
   API вызовов: 3
   Всего изображений использовано: 3
   Ошибок: 0
```

## Интеграция с пайплайном

Новые изображения можно использовать в существующих пайплайнах:

1. Замените оригинальные файлы новыми
2. Обновите `illustrations.json` при необходимости
3. Запустите генерацию видео с новыми изображениями

## Стоимость

Стоимость зависит от модели и количества изображений:
- Оплата только за успешно сгенерированные изображения
- Неудачные запросы не тарифицируются
- Подробности: [Models and pricing](https://www.alibabacloud.com/help/zh/model-studio/models-and-pricing)
