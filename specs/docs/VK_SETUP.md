# 🎬 Настройка VK публикации

Краткая инструкция по настройке публикации на VK Video и VK Audio.

## 📋 Предварительные требования

1. Аккаунт VK
2. Приложение VK (для получения токена доступа)
3. Группа VK (опционально, для публикации в группу)

## 🔧 Настройка приложения VK

### Важно: Изменения в 2025 году
VK больше не позволяет создавать Standalone-приложения. Используйте **плагин-приложение**.

1. Перейдите на [VK Developers](https://vk.com/dev)
2. Создайте новое приложение:
   - **Тип**: Плагин
   - **Название**: любое (например, "BookReader Publisher")
   - **URL**: ваш сайт (например, `https://kreagenium.ru`)
3. Получите `CLIENT_ID` (ID приложения)
4. **CLIENT_SECRET не требуется** для плагин-приложений
5. Добавьте права доступа:
   - `groups` - для работы с группами
   - `video` - для загрузки видео
   
   **Важно**: 
   - Загрузка аудио/музыки полностью недоступна для плагин-приложений VK
   - Публикация в группу недоступна для плагин-приложений VK
   - Доступна только загрузка видео в личный аккаунт
   - Для загрузки аудио и публикации в группу используйте Standalone-приложение

## ⚙️ Настройка конфигурации

1. Откройте `config.publisher.env`
2. Добавьте настройки VK:
```env
VK_CLIENT_ID=ваш_client_id
VK_CLIENT_SECRET=  # Оставьте пустым для плагин-приложений
VK_ACCESS_TOKEN=
VK_GROUP_ID=
VK_TOKEN_PATH=vk_token.json
```

## 🔑 Получение токена доступа

### Автоматический способ (рекомендуется)
```bash
python setup_vk_auth.py
```

### Ручной способ
1. Перейдите по ссылке (замените `ваш_client_id` на ваш ID):
```
https://oauth.vk.com/authorize?client_id=ваш_client_id&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=groups,video&response_type=token&v=5.131
```
2. Войдите в VK и разрешите доступ
3. Скопируйте токен из адресной строки
4. Добавьте токен в `config.publisher.env`:
```env
VK_ACCESS_TOKEN=ваш_токен_доступа
```

**Важно**: Токен для плагин-приложений действует ограниченное время (обычно 24 часа). При необходимости обновите его через `python setup_vk_auth.py`.

## 👥 Настройка группы (опционально)

1. Создайте группу VK или используйте существующую
2. Получите ID группы из URL: `https://vk.com/club123456789` → ID = `123456789`
3. Добавьте ID группы в конфигурацию:
```env
VK_GROUP_ID=123456789
```

## ✅ Проверка настройки

```bash
python test_vk_auth.py
```

## 🚀 Использование

### Пробный запуск
```bash
python publisher.py pipeline_LemEng_87_111 --platforms vk --dry-run
```

### Публикация
```bash
python publisher.py pipeline_LemEng_87_111 --platforms vk
```

### Демонстрация
```bash
./demo_vk_publisher.sh
```

## 📁 Структура пайплайна

VK публикатор автоматически определяет тип контента:
- `video.mp4` → загрузка на VK Video
- `audio.mp3` → загрузка недоступна (плагин-приложения не поддерживают аудио API)
- Оба файла → загружается только видео

## 🔍 Устранение неполадок

### Ошибка аутентификации
- Проверьте токен доступа
- Убедитесь, что токен не истек (для плагин-приложений токен действует ~24 часа)
- Проверьте права доступа (groups, wall, video, audio)
- Обновите токен через `python setup_vk_auth.py`

### Ошибка загрузки
- Проверьте размер файлов (лимит: 2GB для видео, 200MB для аудио)
- Убедитесь в корректности форматов (MP4 для видео, MP3 для аудио)
- Проверьте права доступа к файлам

### Ошибка публикации в группу
- Проверьте ID группы
- Убедитесь, что у вас есть права администратора группы
- Проверьте права токена `wall`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи ошибок
2. Убедитесь в корректности конфигурации
3. Проверьте доступность VK API
4. Обратитесь к [документации VK API](https://vk.com/dev)

